const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const p = await prisma.product.findMany({ where: { title: { contains: 'pumpkin', mode: 'insensitive' } } }); console.log(p); await prisma.$disconnect(); } main();
