const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const titlesToDelete = ['Trinket #90', 'Trinket #91', 'Trinket #92', 'Trinket #93', 'Trinket #94', 'Trinket #95', 'Trinket #96', 'Trinket #97']; const res = await prisma.product.deleteMany({ where: { title: { in: titlesToDelete } } }); console.log(res); await prisma.$disconnect(); } main();
