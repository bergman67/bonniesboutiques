# Context for Explorer Survey 3
Investigate 3D rendering setup:
- `LevitatingProductViewer.tsx` component implementation
- React Three Fiber (`@react-three/fiber`), `@react-three/drei` versions and features (e.g. `<Image>`, `<Billboard>`, materials, textures)
- Current placeholder geometries (spheres, boxes, etc.) and animation loops/levitation physics
- How product data/images are passed into `LevitatingProductViewer`
