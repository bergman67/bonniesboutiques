# Forensic Auditor Context: Integrity Forensics Verification

## Objective
Perform strict, independent forensic integrity verification across all changes made in Milestones 1, 2, and 3:
1. Verify no cheating, mock test fixtures, or hardcoded strings passed off as real functionality:
   - Check `scripts/removeBackgrounds.mjs`: Is it genuinely importing and calling `@imgly/background-removal-node` with real ONNX inference? Are transparent PNGs genuine RGBA images generated by neural segmentation, or pre-rendered dummy stubs?
   - Check `scripts/verify-*.mjs`: Do they perform genuine assertions, or are they trivial `console.log('PASS')` or tautological `assert(true)` facades?
   - Check `src/app/page.tsx`: Is the copy genuinely integrated into the live component hierarchy, or hidden behind dummy flags?
   - Check `src/components/scrollytelling/LevitatingProductViewer.tsx`: Does it genuinely render transparent product cutouts using Drei `<Billboard>` and Three.js materials in the live 3D scene?
2. Examine git diff / file modifications across:
   - `src/app/page.tsx`
   - `src/components/scrollytelling/ScrollytellingExperience.tsx`
   - `src/components/scrollytelling/ProductHUD.tsx`
   - `src/components/scrollytelling/PixelStorefrontLayer.tsx`
   - `src/components/scrollytelling/LevitatingProductViewer.tsx`
   - `src/components/scrollytelling/ScrollyCanvas.tsx`
   - `scripts/removeBackgrounds.mjs`
   - `package.json`
3. Deliver a binary verdict: `CLEAN` or `INTEGRITY VIOLATION` in `handoff.md`.
